<?php

/*$db = new PDO("mysql:dbname=db_npp;host=localhost;", "root");

$st = db->query("select * from users");
if ($st) {
//$res->setFetchMode(PDO::FETCH_ASSOC);
    while ($row = $st->fetch(PDO::FETCH_ASSOC)) {
        print_r($row);
    }
}

$st = $db->prepare("insert into users (username, password, email,status, created_at) values(:name, :password, :email, :status, :created_at)");
$name = "javlon5";
$pass = md5("pass1");
$email = "email5@gmail.com";
//print_r($st);
$db->beginTransaction();
//
if ($st->execute(array(":name" => $name, ":password" => $pass, ":email" => $email, ":status" => "1",":created_at" => "now()"))) {
    echo "success";
}
print_r($st->errorInfo());
//$db->lastInsertId();

$db->commit();*/


/*$st = $db->query("select * from users");
if ($st) {
//$res->setFetchMode(PDO::FETCH_ASSOC);
    while ($row = $st->fetch(PDO::FETCH_ASSOC)) {
        echo implode(",", $row) . "\r\n";
    }
}


*/

/*define("NAME", "sa", true);
echo NAME, ':', Name, ':', nAme;*/

//$htmlData = "this is text of the email body";
//mail("mail@mail.ru","Hello World",$htmlData);
//echo date("Y-m-d H:i:s");
//$link = mysql_pconnect("localhost","root","");
/*$arr = [
    "b" => "a1",
    "a" => "b1",
    "c" => "c1",
    "d" => "d1",
    "e" => "e1",
    "f" => "f1"];
ksort($arr);
print_r($arr);*/
/*echo PHP_INT_MAX ;
var_dump((int)(PHP_INT_MAX + 1));
mcrypt_create_iv();-*/
//T_PAAMAYIM_NEKUDOTAYIM
/*$url = "https://mail.ru/";
print_r(get_headers($url, 1));

$cr = curl_init("https://mail.ru");

curl_setopt($cr, CURLOPT_HEADER, true);
curl_setopt($cr, CURLOPT_RETURNTRANSFER, true);
curl_setopt($cr, CURLOPT_NOBODY, true);
$res = curl_exec($cr);
echo $res;*/
    System.out.print("hello");